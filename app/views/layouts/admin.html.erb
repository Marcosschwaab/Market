<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
	<div id="center">
		<%= link_to 'Melhores Ofertas', admin_product_offers_path %> -
		<%= link_to 'Lista de Produtos', admin_products_path %> - 
		<%= link_to 'Lista de Unidades', admin_units_path %> -
		<%= link_to 'Lista de Gêneros', admin_genders_path %> 
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
 </div>
  </body>
</html>
