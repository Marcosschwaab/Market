<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Mercado Manager 1.2" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
<link rel="shortcut icon" href="../images/market.ico" />
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
		<div id="user_nav">
		  <% if user_signed_in? %>
		    Bem vindo <%= current_user.email %>, mantenha seus produtos atualizados.
		    <%= link_to image_tag('exit.png'), destroy_user_session_path %>
		  <% else %>
		    <%= link_to "Criar Conta", new_user_registration_path %> - <%= link_to "Login", new_user_session_path %>
		  <% end %>
		</div>
		<div id="center">

      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
	</div>
  </body>
</html>
