<%= form_for([:admin, @product]) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nome do Produto" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code, "Código de Barras" %><br />
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :mark, "Marca" %><br />
    <%= f.text_field :mark %>
  </div>
	
	<p><%= f.label :unit_id, "Unidade de Medida" %></p>
  <p><%= f.select :unit_id, @units, :prompt=>"Selecione..." %></p>

	<p><%= f.label :gender_id, "Gênero" %></p>
  <p><%= f.select :gender_id, @genders, :prompt=>"Selecione..." %></p>

  <div class="field">
    <%= f.label :description, "Descrição" %><br />
    <%= f.text_area :description, :rows => 2 %>
  </div>
<br />
  <div class="actions">
   <p><%= f.submit %></p>
  </div>
<% end %>
<br />
